#include <stdio.h>
#include <stlib.h>

int main(){
	char curr, name[64], age[10];
	int i = 0;
	curr = getcharacter();	
		\\get line
	while (curr != '\n'){
		name[i] = curr;
		curr = getcharacter();

	
	
	}
}
